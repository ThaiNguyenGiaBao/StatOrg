name: 'My Custom Chart Action'
description: 'A custom action that gets data and draws a chart'
inputs:
  org-name:
    description: 'Organization name'
    required: true
    default: 'TickLabVN'
runs:
  using: 'node20'
  main: 'index.mjs'
